# Copy this file to .env (project root OR backend/.env). Do NOT commit the real .env.
# IMPORTANT: Rotate (change) any secret you previously exposed publicly.

MONGO_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER_HOST/roadwatch?retryWrites=true&w=majority
JWT_SECRET=REPLACE_WITH_A_NEW_STRONG_RANDOM_STRING
ADMIN_CODE=ADMIN123
PORT=5001

# Tips:
# - Generate JWT_SECRET: `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`
# - If using Atlas, CLUSTER_HOST looks like clustername.xxxxx.mongodb.net
# - Ensure the database name (roadwatch) matches what you expect.